export class Animal{

    constructor(
        public name: string,
        public description: string,
        public year: string,
        public image: string,
        public user: string
    ){

    }
}